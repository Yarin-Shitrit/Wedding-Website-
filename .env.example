# Copy to .env and fill in values
DATABASE_URL="file:./dev.db"

# Admin auth (set a strong secret in production)
AUTH_SECRET="change-me-to-a-long-random-string"
ADMIN_EMAIL="couple@example.com"
ADMIN_PASSWORD="change-me"

# Wedding details shown on the site
NEXT_PUBLIC_BRIDE_NAME="Amit"
NEXT_PUBLIC_GROOM_NAME="Ben"
NEXT_PUBLIC_WEDDING_DATE="2026-09-04T19:00:00+03:00"
NEXT_PUBLIC_VENUE_NAME="The Garden"
NEXT_PUBLIC_VENUE_ADDRESS="123 Main St, Tel Aviv"
NEXT_PUBLIC_VENUE_MAP_URL="https://maps.google.com/?q=Tel+Aviv"

# SnapFinder face-recognition integration (all server-only — the browser never
# calls SnapFinder directly; /api/find-my-photos proxies with a minted token).
# Base URL of the deployed SnapFinder backend (Railway in prod, or
# http://localhost:8000 for a local docker-compose stack).
SNAPFINDER_API_URL="http://localhost:8000"
# UUID of the event row in SnapFinder (response from the admin POST /events).
SNAPFINDER_EVENT_ID="CHANGE_ME_event_id"
# Must match the SnapFinder backend's JWT_SECRET exactly.
SNAPFINDER_JWT_SECRET="CHANGE_ME_jwt_secret"
# Lifetime in seconds of the per-request event JWT minted server-side.
SNAPFINDER_EVENT_TOKEN_TTL="3600"

# Guest photo uploads (/share) — Vercel Blob. Two auth options:
#   A) Static token: paste a read-write token from the Blob store page and uncomment it.
#   B) OIDC (no static secret): set BLOB_STORE_ID + let `vercel env pull` provide VERCEL_OIDC_TOKEN
#      (requires OIDC enabled for the target environment). Deployed prod/preview use this automatically.
BLOB_STORE_ID="store_xxxxxxxxxxxxxxxx"
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Ed25519 public key for verifying Vercel Blob webhook signatures (public, not a secret).
# Multi-line PEM — keep it inside double quotes. Currently unused (no webhook wired up yet).
BLOB_WEBHOOK_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----
CHANGE_ME_base64_public_key
-----END PUBLIC KEY-----
"
